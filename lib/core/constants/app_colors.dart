import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color pine = Color(0xFF2F5D50);       // Primary
  static const Color pineLight = Color(0xFF3F7965);  // Secondary/gradient
  static const Color pinePale = Color(0xFFD1E8DF);   // Light backgrounds
  static const Color mist = Color(0xFFEEF1ED);       // Neutral backgrounds
  static const Color amber = Color(0xFFE2962F);      // Primary CTA/highlights
  static const Color amberPale = Color(0xFFFBEFD9);  // Pending states
  static const Color cream = Color(0xFFFBFBF8);      // Main background

  // Text Colors
  static const Color ink = Color(0xFF1E2A2E);         // Primary text
  static const Color inkMid = Color(0xFF4A5854);      // Secondary text
  static const Color inkMute = Color(0xFF8FA8A0);     // Muted text

  // Feedback Colors
  static const Color danger = Color(0xFFC84B4B);      // Errors/destructive actions
  static const Color dangerPale = Color(0xFFFCE8E8);  // Error backgrounds

  // Neutral UI
  static const Color white = Colors.white;
  static const Color border = Color(0xFFE5E9E7);
  static const Color cardShadow = Color(0x0D1E2A2E);
}
